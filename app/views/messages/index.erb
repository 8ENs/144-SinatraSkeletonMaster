<a href="/messages/new">Post a message... It's FREE (for now)!</a>

<% @messages.each do |message| %>
  <div class="message">
    <p>
      <%= message.content %> <small>@<a href="/messages/<%= message.id %>"><%= message.author %></a></small>
    </p>
  </div>
<% end %>